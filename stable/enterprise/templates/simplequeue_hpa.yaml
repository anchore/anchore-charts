{{- if .Values.simpleQueue.autoscaling.enabled }}
{{- $component := "simpleQueue" -}}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ template "enterprise.simpleQueue.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels: {{- include "enterprise.common.labels" (merge (dict "component" $component) .) | nindent 4 }}
  annotations: {{- include "enterprise.common.annotations" (merge (dict "component" $component) .) | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ template "enterprise.simpleQueue.fullname" . }}
  minReplicas: {{ .Values.simpleQueue.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.simpleQueue.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.simpleQueue.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.simpleQueue.autoscaling.targetCPUUtilizationPercentage }}
  {{- end }}
  {{- if .Values.simpleQueue.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.simpleQueue.autoscaling.targetMemoryUtilizationPercentage }}
  {{- end }}
{{- end }}
