suite: UI HPA Tests
templates:
  - ui_hpa.yaml
release:
  name: test-release
  namespace: test-namespace
chart:
  version: 9.9.9
  appVersion: 9.9.9
set:
  anchoreConfig.policy_engine.vulnerabilities.matching.exclude.providers: []
  anchoreConfig.policy_engine.vulnerabilities.matching.exclude.package_types: []

tests:
  - it: should not render HPA by default
    asserts:
      - hasDocuments:
          count: 0

  - it: should render HPA when autoscaling is enabled
    set:
      ui.autoscaling.enabled: true
      ui.autoscaling.minReplicas: 2
      ui.autoscaling.maxReplicas: 5
      ui.autoscaling.targetCPUUtilizationPercentage: 70
    asserts:
      - hasDocuments:
          count: 1
      - isKind:
          of: HorizontalPodAutoscaler
      - equal:
          path: apiVersion
          value: autoscaling/v2
      - equal:
          path: metadata.name
          value: test-release-enterprise-ui
      - equal:
          path: spec.scaleTargetRef.apiVersion
          value: apps/v1
      - equal:
          path: spec.scaleTargetRef.kind
          value: Deployment
      - equal:
          path: spec.scaleTargetRef.name
          value: test-release-enterprise-ui
      - equal:
          path: spec.minReplicas
          value: 2
      - equal:
          path: spec.maxReplicas
          value: 5
      - contains:
          path: spec.metrics
          content:
            type: Resource
            resource:
              name: cpu
              target:
                type: Utilization
                averageUtilization: 70

  - it: should render memory metric when targetMemoryUtilizationPercentage is set
    set:
      ui.autoscaling.enabled: true
      ui.autoscaling.minReplicas: 1
      ui.autoscaling.maxReplicas: 3
      ui.autoscaling.targetCPUUtilizationPercentage: 80
      ui.autoscaling.targetMemoryUtilizationPercentage: 75
    asserts:
      - contains:
          path: spec.metrics
          content:
            type: Resource
            resource:
              name: memory
              target:
                type: Utilization
                averageUtilization: 75

  - it: should render correct labels
    set:
      ui.autoscaling.enabled: true
      ui.autoscaling.minReplicas: 1
      ui.autoscaling.maxReplicas: 3
      ui.autoscaling.targetCPUUtilizationPercentage: 80
    asserts:
      - isSubset:
          path: metadata.labels
          content:
            app.kubernetes.io/name: test-release-enterprise
            app.kubernetes.io/component: ui
