securityContext:
  fsGroup: null
  runAsGroup: null
  runAsUser: null
ui-redis:
  master:
    podSecurityContext:
      enabled: false
    containerSecurityContext:
      enabled: false
analyzer:
  extraEnv:
    - name: HOME
      value: /tmp

anchoreConfig:
  default_admin_password: foobar

postgresql:
  chartEnabled: false
  externalEndpoint: "anchore-db-rw.anchore.svc:5432"
  auth:
    username: anchore
    password: anchore-postgres,123
    database: anchore
